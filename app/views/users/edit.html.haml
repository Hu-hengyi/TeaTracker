-content_for(:left_bar_title, "Update " + @user.name)
-content_for(:left_bar_content) do
  = link_to 'Back', users_path, :class => "left_bar_button"

= render 'form'

= link_to 'Show', user_path(@user)
\|
= link_to 'Back', users_path
