-content_for(:left_bar_title, "Update " + @pg.name)
-content_for(:left_bar_content) do
  = link_to 'Back', pay_groups_path, :class => "left_bar_button"
  = link_to 'Show collection point', @pg, :class => "left_bar_button"

= render 'form'

