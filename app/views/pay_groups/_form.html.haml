= form_for @pg, :html => {:class => "general-form"} do |f|
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :notes
    = f.text_field :notes
  .actions
    = f.submit :id => :create_pay_group, :class => "green-btn"
