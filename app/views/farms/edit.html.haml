-content_for(:left_bar_title, "Update " + @farm.name)
-content_for(:left_bar_content) do
  = link_to 'Back', farms_path, :class => "left_bar_button"
  = link_to 'Show farm', @farm, :class => "left_bar_button"

= render 'form'

